"""Airtable synchronisation — the outbox, the worker, and the status surface.

Design package: `ifet-firmware/docs/labos-airtable/design/integration-design-2026-09-05.md`.
Nothing here is deployed; the milestone in flight is the local outbox and its
offline-recovery behaviour, exercised entirely against synthetic fixtures.
"""
